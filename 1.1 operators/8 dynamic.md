#### dynamic not an operator


#### var : type inference for local variables : done by compiler


#### dynamic : converts to object type

                dynamic d = 10;
                Console.WriteLine(d);
                d = "hello";
                Console.WriteLine(d);
              
